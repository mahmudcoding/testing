# Published reports

One row per published run, appended when you publish rather than at the end of the
run — a session that dies otherwise loses its row. The artifact URL is what makes a
report readable later: record it here and in the run's `artifact:` field, so a
later session can pass it back as `url` and update the same artifact in place.

| file | artifact URL | session |
|---|---|---|
